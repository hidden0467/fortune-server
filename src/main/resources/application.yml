server:
  port: ${PORT:8080}

spring:
  application:
    name: fortune-server

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
