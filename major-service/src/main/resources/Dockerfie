# Dockerfile
# 使用官方 OpenJDK 17 镜像
FROM openjdk:17

# 设置工作目录
WORKDIR /app

# 拷贝构建好的 jar 文件到镜像
COPY target/fortune-server.jar app.jar

# 暴露应用端口（Spring Boot 默认 8080）
EXPOSE 8080

# 启动命令
ENTRYPOINT ["java", "-jar", "app.jar"]
